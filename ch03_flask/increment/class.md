# 클릭 시 수 증가 웹 사이트
## 화면 구성
1. 학생들에게 예제 화면을 보여주고 "늬들이 html과 js를 사용해서 만들어봐라"
  * 1차적으로 ai 없이 만들어보라 한 뒤, 일정 시간이 지나도 안된다? ai 돌려라
2. 백앤드 구성이 끝나고 접속이 잘 되면  '전송' 버튼 클릭하면 호출되는 get 방식 '/<num>' 호출하기 기능 추가
## 백엔드 구성
1. 애들이 화면 구성이 끝나면 '/'로 들어오면 templates/index.html가 보여지게 만드는 render_template 소개, 코드 복붙
2. 접속 확인
3. '전송' 버튼 클릭하면 호출되는 get 방식 '/<num>' api 만들어보기
  * 넘어오는거 확인되었으면 디비 구축 & pymysql로 호출
## 데이터베이스
1. 화면과 백앤드가 모두 구성이 완료되었으면 num을 저장하기 위한 테이블 구성
